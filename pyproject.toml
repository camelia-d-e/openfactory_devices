[tool.ruff]
line-length = 88
target-version = "py312"
exclude = [
    "wtvb01/device_model.py",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"*/tests/*" = ["S101"]

[tool.pytest.ini_options]
asyncio_mode = "auto"